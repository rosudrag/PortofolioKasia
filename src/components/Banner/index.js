import React from 'react';
import asMainComponent from '../../common/asMainComponent';

// import styles from './index.css';

const Banner = (props) => {
    return (
      <p>test</p>
    );
};

export default asMainComponent(Banner);
